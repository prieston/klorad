{
  "buildCommand": "cd ../.. && pnpm vercel:build:heritage",
  "devCommand": "pnpm dev:heritage",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --prod=false",
  "framework": "nextjs",
  "regions": ["fra1"],
  "env": {
    "NEXT_PUBLIC_VERCEL_ENV": "production"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "outputDirectory": ".next"
}
