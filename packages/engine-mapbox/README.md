# @klorad/engine-mapbox

Mapbox GL JS viewer integration for Klorad campus / map-first projects.

Observation points use `[lng, lat, altitudeMeters]` for `position` and `target`.

Set `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN` in the editor app.
